name: Matrix-Example-CI
on:
  workflow_dispatch:  
  push:
  pull_request:

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.changed-files.outputs.all_changed_files }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v47
        with:
          matrix: true
          files_yaml: |
            frontend:
              - 'frontend/**'
            backend:
              - 'backend/**'
            database:
              - 'database/**'
            docs:
              - 'docs/**'
      - name: Show output
        run: |
          echo "${{ toJSON(steps.changed-files.outputs) }}"
        shell:
          bash
  # matrix-job:
  #   name: Run Matrix Job
  #   runs-on: ubuntu-latest
  #   needs: [detect-changes]
  #   strategy:
  #     matrix: 
  #       files: ${{ fromJSON(needs.detect-changes.outputs.matrix) }}
  #     max-parallel: 4
  #     fail-fast: false
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v5
  #     - name: Test
  #       run: |
  #         echo ${{ matrix }}